# Maki — the music server. muse-web talks to it directly (Maki's CorsLayer is
# permissive, see maki/src/main.rs:133), so no BFF/proxy is needed.
VITE_MAKI_URL=http://localhost:3033

# umi — optional lyrics service. Frontend fails silently without it.
VITE_UMI_URL=http://localhost:3032
